# Built outputs
/build/
lib/build/
.gradle/

# Android Studio
*.iml
.idea/
local.properties

# Generated native libs + UniFFI bindings — downloaded from trueseal-sync releases by CI.
# release.yml force-adds these for each tag so JitPack can build the AAR.
lib/src/main/jniLibs/**/*.so
lib/src/main/java/uniffi/**/*.kt

# macOS
.DS_Store

# Gradle wrapper jar (add to git if you want hermetic builds without pre-installed Gradle)
# gradle/wrapper/gradle-wrapper.jar

# Rust build artefacts (never committed — live in CARGO_TARGET_DIR)
/target/
