# Built outputs
/build/
lib/build/
.gradle/

# Android Studio
*.iml
.idea/
local.properties

# Native build artefacts (generated by scripts/build-android.sh — commit after running)
# Uncomment the lines below if you want to ignore .so files (not recommended — JitPack needs them)
# lib/src/main/jniLibs/**/*.so

# macOS
.DS_Store

# Gradle wrapper jar (add to git if you want hermetic builds without pre-installed Gradle)
# gradle/wrapper/gradle-wrapper.jar

# Rust build artefacts (never committed — live in CARGO_TARGET_DIR)
/target/
